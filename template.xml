<template 
    format="4"
    revision="1"
    name="Redux"
    description="Create classes for Redux">

    <category value="Other"/>

    <parameter 
        id="className"
        name="Class Name"
        type="string"
        constraints="class|unique|nonempty"
        help="The name of the Redux classes"/>

    <parameter
        id="addStub"
        name="Add stubs to the classes for awoiding of errors"
        type="boolean"
        default="true"
        help="If true, stub code will be added to the classes. Otherwise, generated classes will contain errors." /> 

    <globals file="globals.xml.ftl" />
    <execute file="recipe.xml.ftl" />

</template>